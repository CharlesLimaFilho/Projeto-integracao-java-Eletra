default:
  image: maven:3.9.12-amazoncorretto-8-alpine

stages:
  - pre-build
  - build

download-javafx:
  stage: pre-build
  script:
    - apt-get update -y
    - apt-get install -y openjfx
  artifacts:
    paths:
      - builds/

build-front:
  stage: build
  script:
    - cd project-integration
    - 'mvn clean package'
