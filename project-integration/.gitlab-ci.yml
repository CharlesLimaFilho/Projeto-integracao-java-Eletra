default:
  image: maven:3.6.3-jdk-8

stages:
  - build

build-front:
  stage: build
  script:
    - apt-get update
    - apt-get install -y openjfx
    - cd project-integration
    - 'mvn clean package'
